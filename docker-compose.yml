services:
  flashcard-gen:
    build: .
    ports:
      - "8000:8000"
    environment:
      - BASE_URL=${BASE_URL:-http://localhost:8000}
      - MODEL=${MODEL:-gpt-5-nano}
      - API_KEY=${API_KEY}